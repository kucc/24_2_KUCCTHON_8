// 이거 기반으로 해서,
const data = [];

/**
 * 
data object 반환해주기
 */
export const makeCharacter = (answers, name) => {
  return {
    star: '헤라클래스',
    image: './images/stars/heracles.png',
    whoNamed: name,
  };
};
